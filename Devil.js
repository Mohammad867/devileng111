```const {discord,client,richembad;atthhment} = require ("discord.js")

const client  = new client ({disableEverony:true});

client ("ready",)() = > {

console.log (logged in as $)   

{client.user.username}!')

client.user.seractivity(shell;)[type]:palaying{()}
(lera shtik bnusa bo zher botakka)

client.on ("gliudmemebradd,member = > {")
let m membar.user,

var embed = new richembad ()
.settitle ("Systam bot")

.addfiled (":,nawe membar"), membar.user 

.addfiled thubanil(m.avatarUrl)

.setiamg("")

.set colord ("RANDOM")

.set timestamp();


var channel = member. guild.channels.get ("")

if (!channel) return;

channel.send({embed;embad});

client.on (massage",async massage = > {})

let prefix = "$"; // lera prifex bot da bne ///


const args = massge.content.slice(prefix.lenght).trim().split(/s+/g);


const command = args.shift().tolowercase();

if(!massage.content.startswitch(prefix)massage.author.bot) return;

if (command=="shell")] 

client .emit (gulidmemberadd;massage.membar)



return message.channel.send()



client.login("")



///by shell // dastkari code maka //// code welcome sada
/// ba hiway swd ```
